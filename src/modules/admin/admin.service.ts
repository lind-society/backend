import { Injectable } from '@nestjs/common';

@Injectable()
export class AdminService {
  constructor() {}
  // notes : exclude password and refresh token
  
  // view it's own detail (view profile)

  // view other admin detail

  // view all admin
}
