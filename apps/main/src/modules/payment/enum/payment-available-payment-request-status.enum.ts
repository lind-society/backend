export enum PaymentAvailableRequestStatus {
  AcceptingPayments = 'ACCEPTING_PAYMENTS',
  RequiresAction = 'REQUIRES_ACTION',
  Authorized = 'AUTHORIZED',
  Cancelled = 'CANCELED',
  Expired = 'EXPIRED',
  Succeeded = 'SUCCEEDED',
  Failed = 'FAILED',
}
