export enum MainProvider {
  Whatsapp = 'WHATSAPP_PROVIDER',
  PaymentGateway = 'PAYMENT_GATEWAY_PROVIDER',
  Storage = 'STORAGE_PROVIDER',
}

export enum WhatsappClientProviders {
  Main = 'MAIN_WHATSAPP_CLIENT',
}

export enum StorageClientProvider {
  GCP = 'GCP_CLIENT',
}
